# Installation
composer install

# Tests

## Tests description

The main test is: testPlateau 
The other tests are dedicated to non-main functions

## Run tests

In the folder marsroverchallenge, run the command:
./vendor/bin/phpunit tests